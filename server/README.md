# Server

Node.js + Express + SQLite service.

## Scripts

```bash
yarn install
yarn dev
```

## Endpoints

- `GET /health`
- `GET /api/cards`
- `POST /api/cards`
- `PATCH /api/cards/:id`
- `DELETE /api/cards/:id`
